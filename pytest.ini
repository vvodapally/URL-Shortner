# pytest.ini
[pytest]
asyncio_mode = auto
testpaths = tests
markers =
    unit: Pure unit tests — no running services required
    integration: Requires Postgres + Redis (docker compose up postgres redis)
    e2e: End-to-end tests against running stack (docker compose up)
log_cli = true
log_cli_level = WARNING
