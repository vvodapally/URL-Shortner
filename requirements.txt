# requirements.txt — URL Shortener service
# Pinned to minor versions; patch updates allowed via pip --upgrade

# ── Web framework ──────────────────────────────────────────────────────
fastapi==0.115.0
uvicorn[standard]==0.30.6          # includes uvloop + httptools
httpx==0.27.2                      # async HTTP client (tests + health checks)

# ── Database ───────────────────────────────────────────────────────────
sqlalchemy[asyncio]==2.0.35
asyncpg==0.29.0                    # fastest async PostgreSQL driver
alembic==1.13.3                    # database migrations

# ── Redis ──────────────────────────────────────────────────────────────
redis[asyncio]==5.0.8

# ── Validation ─────────────────────────────────────────────────────────
pydantic==2.9.2
pydantic-settings==2.5.2

# ── Geo-lookup (Brownfield Scenario 2) ────────────────────────────────
# geoip2==4.8.0                    # uncomment when MaxMind DB is available

# ── Testing ────────────────────────────────────────────────────────────
pytest==8.3.3
pytest-asyncio==0.24.0
pytest-cov==5.0.0

# ── Dev tools ──────────────────────────────────────────────────────────
python-dotenv==1.0.1               # load .env in development
